pipeline {
	agent any

	stages {
		stage ("Build the package") {
			steps {
				node ("build-ser") {
					print ("Clone the git repo")
					checkout scm

					print ("Building the war package")
					sh "mvn install"
								}
							}
					}
			}
		}
